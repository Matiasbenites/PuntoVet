import { Typography } from "@mui/material"


export const TituloFormularioPrincipales = ({ titulo }) => {
    return (
        <Typography variant="h2">
            {titulo}
        </Typography>
    )
}